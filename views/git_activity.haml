%html
	%head
		%title Git - The Furnace
	%body
		%h1 Git activity
		%ul
			- log.each do |entry|
				%li
					%img{:src => entry['actor']['avatar_url'] + '&s=32'}
					%span= entry['type']
					- (entry['payload']['commits'] || []).each do |commit|
						%p= commit['message']
